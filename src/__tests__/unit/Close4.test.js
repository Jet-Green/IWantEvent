import { describe, it, } from "vitest"
import { render } from "@testing-library/vue"

import Close4 from '../../components/CreateEvent/Steps/CloseEvent/Close4.vue'

describe('Close4 tests', () => {
    it('should render', async () => {
        const wrapper = render(Close4)
    })
})